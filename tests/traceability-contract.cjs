'use strict';const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const file=path.join(root,'tests/TRACEABILITY.json');if(!fs.existsSync(file)){console.error('Missing tests/TRACEABILITY.json copied from execution package');process.exit(1);}const x=JSON.parse(fs.readFileSync(file));const rows=x.rows||[];const bad=rows.filter(r=>!r.requirement_id||!r.decision_ids?.length||!r.scenario_ids?.length||!r.test_names?.length);if(bad.length){console.error('Incomplete traceability '+bad.map(x=>x.requirement_id));process.exit(1);}console.log(`PASS traceability ${rows.length} rows`);
