'use strict';const fs=require('fs'),path=require('path');const root=path.resolve(__dirname,'..');const forbidden=['CLAUDE.md','.claude','.mcp.json','AGENTS.md'];const found=forbidden.filter(p=>fs.existsSync(path.join(root,p)));if(found.length){console.error('ACTIVE_LEGACY_CONTROL_PLANE '+found.join(','));process.exit(1);}console.log('PASS no active repository-resident Claude control plane');
